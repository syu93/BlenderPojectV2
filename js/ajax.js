function send_scene(scene){
	// alert(scene);
	var send={};
	send.scene = scene;
	// alert(send.scene);
	
	// $.ajax({
		// type: "POST",
		// url: "../core/init.php",
		// data: send,
		// success: function(data){
			// alert(data);
		// }, 
		// dataType: "text"
	// });
}

$( document ).ready(function(){
});